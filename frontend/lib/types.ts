export type Tour = { id:string; title:string; slug:string; location:string; base_price:number; duration_days:number; cover_image:string };
export type Category = { id:string; name:string; slug:string };
export type Booking = { booking_no:string; status:string; total_price:number };